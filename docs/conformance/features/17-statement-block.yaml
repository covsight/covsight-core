# UCIS conformance registry -- section 17: Statement / block coverage
#
# GENERATED SKELETON, HAND-MAINTAINED THEREAFTER.
# Records intent only: what the backend is expected to do with each feature.
# Status ("is it tested?", "is it documented?") is computed by
# tools/gen_conformance_matrix.py and never written here.
#
# See docs/conformance/README.md.

section:
  number: 17
  title: "Statement / block coverage"
  spec_ref: "§ 6.5.3, § 9.12"

features:
  - id: SB.1
    title: "`UCIS_BLOCK` scope + `UCIS_STMTBIN` per line/statement"
    spec_ref: "§ 6.5.3"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: SB.2
    title: "`UCIS_BLOCKBIN` for block-level aggregate"
    spec_ref: "§ 6.5.3"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: SB.3
    title: "`UCIS_INT_STMT_INDEX` integer property"
    spec_ref: "§ 6.5.3"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: SB.4
    title: "XML schema: `STATEMENT`, `PROCESS_BLOCK`, `BLOCK`"
    spec_ref: "§ 9.12"
    kind: feature
    backends:
      parquet:
        scope: out_of_scope
        reason: >
          XML interchange is a separate backend (covsight.core.xml) with its own
          format plugin. Conversion runs through the UCIS object API, so the
          Parquet mapping neither reads nor writes XML.

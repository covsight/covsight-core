# UCIS conformance registry -- section 16: Branch coverage
#
# GENERATED SKELETON, HAND-MAINTAINED THEREAFTER.
# Records intent only: what the backend is expected to do with each feature.
# Status ("is it tested?", "is it documented?") is computed by
# tools/gen_conformance_matrix.py and never written here.
#
# See docs/conformance/README.md.

section:
  number: 16
  title: "Branch coverage"
  spec_ref: "§ 6.5.2, § 9.13"

features:
  - id: BR.1
    title: "`UCIS_BRANCH` scope with `UCIS_BRANCHBIN` children"
    spec_ref: "§ 6.5.2"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: BR.2
    title: "`UCIS_INT_BRANCH_HAS_ELSE`, `UCIS_INT_BRANCH_ISCASE`"
    spec_ref: "§ 6.5.2"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: BR.3
    title: "`UCIS_IS_BR_ELSE` coveritem flag (else-branch marker)"
    spec_ref: "§ 6.5.2"
    kind: feature
    backends:
      parquet:
        scope: core

  - id: BR.4
    title: "XML schema: `BRANCH_STATEMENT`, `BRANCH`"
    spec_ref: "§ 9.13"
    kind: feature
    backends:
      parquet:
        scope: out_of_scope
        reason: >
          XML interchange is a separate backend (covsight.core.xml) with its own
          format plugin. Conversion runs through the UCIS object API, so the
          Parquet mapping neither reads nor writes XML.
